package paxos.detail.protocol.pb;


enum command_type
{
        START_ELECTION = 0;
        MY_ELECTION_ID = 1;
        WON_ELECTION = 2;
};

message command
{
        optional command_type type      = 1;
        optional string host_id         = 2;
}
